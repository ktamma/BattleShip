@page
@model WebApp.Pages_TouchRules.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.TouchRule[0].Rule)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.TouchRule) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Rule)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.TouchRuleId">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.TouchRuleId">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.TouchRuleId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
